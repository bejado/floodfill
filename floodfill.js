function floodFill(x, y, r, g, b) {
  /*
    Implement the flood fill algorithm.

    Functions you have available to you:
      - drawPixel(x, y, r, g, b)
      - getPixel(x, y): returns color.r, color.g, color.b

    Constants:
      CANVAS_WIDTH, CANVAS_HEIGHT
  */
  drawPixel(x, y, r, g, b);
}
